/** Hero subtitle (tagline) motion — keep in sync with `Hero` tagline `motion.div`. */
export const HERO_TAGLINE_ENTER_DELAY_S = 0.8;
export const HERO_TAGLINE_ENTER_DURATION_S = 0.42;

/** Delay (seconds) before the sticky header fades in; photos entrance uses the same beat. */
export const SITE_HEADER_ENTER_DELAY_S = 0.88;

/** Header opacity fade duration (seconds). */
export const SITE_HEADER_FADE_DURATION_S = 0.34;

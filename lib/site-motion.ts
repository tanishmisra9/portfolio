/** Hero subtitle (tagline) motion — keep in sync with `Hero` tagline `motion.div`. */
export const HERO_TAGLINE_ENTER_DELAY_S = 0.56;
export const HERO_TAGLINE_ENTER_DURATION_S = 0.29;

/** Header opacity fade duration (seconds). */
export const SITE_HEADER_FADE_DURATION_S = 0.24;

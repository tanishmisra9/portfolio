/** Per-quote random start delay window (ms) — the stagger across the cloud. */
export const MAX_START_DELAY_MS = 2500;

/** Fade + scale entrance per quote. */
export const QUOTE_FADE_DURATION_S = 0.5;
export const QUOTE_FADE_SCALE_FROM = 0.96;

/** Reduced motion: a single soft fade, no stagger, no scale. */
export const REDUCED_MOTION_FADE_DURATION_S = 0.45;
